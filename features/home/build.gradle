apply from: '../../gradle/script/feature_dependencies.gradle'
apply plugin: "androidx.navigation.safeargs.kotlin"
apply plugin: 'kotlin-kapt'

android {
    defaultConfig {
        testInstrumentationRunner "com.mkdev.core_test.FakeRunner"
    }
    dataBinding {
        enabled true
    }
    testOptions {
        animationsDisabled = true
    }
}

dependencies {
    // GLIDE
    implementation Libraries.GLIDE
    implementation Libraries.GLIDE_COMPILER
}
